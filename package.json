{
  "name": "simply1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd my-app && npm run build",
    "start": "cd my-app && npm start",
    "install-deps": "cd my-app && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
